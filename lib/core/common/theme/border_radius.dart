import 'package:flutter/material.dart';

class AppBorderRadius {
  static BorderRadius BORDER_RADIUS_3 =
      const BorderRadius.all(Radius.circular(3));
  static BorderRadius BORDER_RADIUS_4 =
      const BorderRadius.all(Radius.circular(4));
  static BorderRadius BORDER_RADIUS_5 =
      const BorderRadius.all(Radius.circular(5));
  static BorderRadius BORDER_RADIUS_4_93 =
      const BorderRadius.all(Radius.circular(4.93));
  static BorderRadius BORDER_RADIUS_6 =
      const BorderRadius.all(Radius.circular(6));
  static BorderRadius BORDER_RADIUS_7 =
      const BorderRadius.all(Radius.circular(7));
  static BorderRadius BORDER_RADIUS_8 =
      const BorderRadius.all(Radius.circular(8));
  static BorderRadius BORDER_RADIUS_9 =
      const BorderRadius.all(Radius.circular(9));
  static BorderRadius BORDER_RADIUS_10 =
      const BorderRadius.all(Radius.circular(10));
  static BorderRadius BORDER_RADIUS_19 =
      const BorderRadius.all(Radius.circular(19));
  static BorderRadius BORDER_RADIUS_24_12 = const BorderRadius.only(
    topLeft: Radius.circular(24.12),
    topRight: Radius.circular(24.12),
  );
  static BorderRadius BORDER_RADIUS_50 =
      const BorderRadius.all(Radius.circular(50));
  static BorderRadius BORDER_RADIUS_26 =
      const BorderRadius.all(Radius.circular(26));
  static BorderRadius BORDER_RADIUS_22 =
      const BorderRadius.all(Radius.circular(22));
  static BorderRadius BORDER_RADIUS_25 =
      const BorderRadius.all(Radius.circular(25));
  static BorderRadius BORDER_RADIUS_27 =
      const BorderRadius.all(Radius.circular(27));
  static BorderRadius BORDER_RADIUS_65 =
      const BorderRadius.all(Radius.circular(65));
  static BorderRadius BORDER_RADIUS_45 =
      const BorderRadius.all(Radius.circular(45));
  static BorderRadius BORDER_RADIUS_12 =
      const BorderRadius.all(Radius.circular(12));
}
